<UserControl x:Class="Chime.TrackProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chime"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel LastChildFill="True" Margin="10,10,10,0" VerticalAlignment="Top">
            <Label x:Name="taskName" Content="" HorizontalAlignment="Left" VerticalAlignment="Top" DockPanel.Dock="Left" Margin="0,0,10,0"/>
            <ProgressBar x:Name="taskProgress" Maximum="1" SmallChange="0.01" LargeChange="0.1"/>
        </DockPanel>
    </Grid>
</UserControl>
